spring.application.name=sustainiq

# Backend runs on http://localhost:8080
server.port=8080

# In-memory storage only for this MVP — no database is configured.
# The service layer is structured so a MySQL (Spring Data JPA) repository can be
# added later without changing the controllers.
